<%= form_with(model: [ @series, @series.books.build ], local: true) do |form| %>
<!--TODO: Fix this so it populates the edit-->

  <% if @series.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@series.errors.count, "error") %> prohibited this series from being saved:
      </h2>
      <ul>
        <% @series.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :series_number %><br>
    <%= form.text_field :series_number %>
  </p>
  <p>
    <%= form.label :year_published %><br>
    <%= form.text_area :year_published %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>